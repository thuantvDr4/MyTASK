import * as t from './actionTypes';

let initialState = {
    objCustomerInfo : null,
};

const listCustomerInfo = (state = initialState, action) => {
    switch (action.type) {
        default:
            break;
    }
    return state;
};

export default listCustomerInfo;
